name: Deploy Node Application

on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Debug Docker Username
        run: echo ${{ secrets.DOCKER_USERNAME }}

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract Git Commit SHA
        id: get_sha
        run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build Docker image
        run: docker build -t sridzar007/react-frontend:${{ env.sha }} .

      - name: Publish image to Docker Hub
        run: docker push sridzar007/react-frontend:${{ env.sha }}
 
  # deploy:
  #   needs: build
  #   runs-on: self-hosted 
  #   steps:
  #     - name: Pull Image from Docker Hub
  #       run: docker pull sridzar007/react-frontend:latest 
      
  #     - name: Delete Old Container
  #       run: docker rm -f reactjs-app-container || true
      
  #     - name: Run Docker Container
  #       run: docker run -d -p 3000:80 --name reactjs-app-container sridzar007/react-frontend:latest
